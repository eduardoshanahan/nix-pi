# Local-only config
config/local.env

# Secrets or private data
private/

# Private host overrides
nixos/hosts/private/
!nixos/hosts/private/README.md

# Nix build symlinks
result
result-*

# Local SD image artifacts (keep in repo for syncing, but don't commit)
sd-image/*.img
sd-image/*.img.zst
sd-image/*.sha256

# OS / editor
.DS_Store
